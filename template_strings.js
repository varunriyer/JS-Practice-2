var text = `Hello World!`;
console.log(text);

console.log(`We can now use "quotes" inside a 'string'!!`);

console.log(
    `It 
also 
allows
multiline
strings`);

text = "string";
console.log(`One of the main use cases of template strings is ${text} interpolation`);

cp = 10;
sp = 17;
console.log(`The profit is ${sp - cp}`); //it also allows expressions in the string as well


